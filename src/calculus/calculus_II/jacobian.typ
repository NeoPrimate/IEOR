== Jacobian

For a vector valued function

$
  g(x) = vec(
    g_1 (x),
    g_2 (x),
    dots.v,
    g_m (x),
  ) quad g: RR^n arrow RR^m
$

The Jacobian:

$
  J_g (x) = vec(
    gradient g_1 (x)^T,
    gradient g_2 (x)^T,
    dots.v,
    gradient g_m (x)^T,
  )
$