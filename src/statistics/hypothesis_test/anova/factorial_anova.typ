= Factorial ANOVA

#align(center)[
  #table(
    columns: 2,
    inset: 1em,
    [One-Way], [1 Variable],
    [Two-Way], [2 Variables],
    [Factoial], [$n$ Variables],
  )
]

$
  "SS"_"total"
    = sum_(emptyset != S subset.eq {1, ..., n}) "SS"_S
    + "SS"_"within"
$