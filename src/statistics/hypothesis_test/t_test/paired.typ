#import "../../../utils/examples.typ": eg
#import "../../../utils/code.typ": code

=== Paired

$
t = overline(D) / (s_D / sqrt(n))
$

- $overline(D)$: mean of the differences between paired observations
- $s_D$: standard deviation of the differences
- $n$: number of pairs