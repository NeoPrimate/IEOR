== Multiple regression

$
Y = beta_0 + beta_1 x_1 + beta_2 x_1 + ... + beta_n x_n + epsilon
$

Matrix form:

$
Y = X beta + epsilon
$

Estimating Coefficients (OLS):

$
hat(beta) = (X^T X)^(-1) X^T Y
$

Where:
- $hat(beta)$: vector of estimated coefficients

