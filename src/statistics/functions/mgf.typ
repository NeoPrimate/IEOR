#import "/lib/imports.typ": *
#show: formatting

== MGF (Moment-Generating Function)

Summarize a probability distribution of a random variable by encoding all of its moments (like mean, variance, skewness, etc.) into a single function.

$
M_X(t) = E[e^(t X)]
$

