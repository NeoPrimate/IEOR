#import "/lib/imports.typ": *
#show: formatting

= Mann–Whitney U <statistics_non_parametric_mann_whitney_u>

Determine whether there is a significant difference between the distributions of two independent samples (used as an alternative to the independent samples t-test when the assumptions of normality are not met)
